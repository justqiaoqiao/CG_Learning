# 使用 Taichi 实现 3D 变换

一个简单、可交互的 3D 变换演示程序，基于 **Taichi** 实现，直观展示 MVP（模型-视图-投影）变换流程。  
你可以通过键盘控制三角形绕 Z 轴旋转，观察 3D 坐标如何映射到 2D 屏幕上。

## 功能特点

- **模型变换**：三角形绕 Z 轴旋转。
- **视图变换**：相机固定在指定位置。
- **透视投影**：将 3D 点转换为 2D 屏幕坐标。
- **实时交互**：按 `A` / `D` 键左右旋转三角形。
- **视觉反馈**：三角形以 RGB 三色边显示。

